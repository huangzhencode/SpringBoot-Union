server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server #eureaka-server实例的名字
  client:
    register-with-eureka: false #不把自己注册进eureka
    fetch-registry: false #不从eureka上获取服务的注册信息
    service-url:
      defaultZone: http://localhost:8761/eureka/